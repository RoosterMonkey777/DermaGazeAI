<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp">

    <!-- Title -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Your Medications"
        android:textSize="24sp"
        android:textStyle="bold"
        android:layout_gravity="center_horizontal"
        android:paddingBottom="16dp"/>

    <!-- Message to display when no medications are added -->
    <TextView
        android:id="@+id/no_medications_message"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="You currently have no medications added. Search our database of 55000+ medications provided by Health Canada."
        android:textSize="18sp"
        android:layout_gravity="center_horizontal"
        android:paddingTop="24dp"
        android:visibility="gone"/>

    <!-- RecyclerView to display added medications -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/medication_recycler_view"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:layout_marginTop="16dp" />

    <!-- Floating action button to add new medications -->
    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/add_medication_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="end"
        android:layout_marginBottom="80dp"
        android:layout_marginEnd="20dp"
        android:src="@drawable/baseline_search_50"
        android:contentDescription="Add Medication"/>
</LinearLayout>
